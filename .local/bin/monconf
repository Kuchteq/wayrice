#!/bin/sh

$EDITOR -c "map <enter> :w<CR>" -c "autocmd BufWritePost * silent !kanshictl reload && notify-send '󰍺  Config applied' || notify-send ' 󰍺  Config failed'" -c "set filetype=config" "$XDG_CONFIG_HOME/kanshi/config"
