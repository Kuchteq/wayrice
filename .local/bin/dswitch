pgrep -x dwm && sudo chvt 2 || sudo openvt -f -c2 -- su maniu -c 'export XDG_CONFIG_HOME=$HOME/.config GDK_BACKEND=x11 DISPLAY=:2 GDK_DPI_SCALE=1 QT_AUTO_SCREEN_SCALE_FACTOR=1 && startx -display :2'

