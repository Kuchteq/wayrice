[binds]

n = next
p = prev

<Ctrl+r> = rotate by 90

a = zoom actual
r = reset
<Ctrl+o> = exec foot -o environment.START_IN_LFCD=true -o environment.LFCD_FOCUSPATH="$imv_current_file" & kill $imv_pid; 

