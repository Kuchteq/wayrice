# You can add comments to these files with #
h   $HOME
at  ${XDG_DESKTOP_DIR:-$HOME/atm} # at the moment
ds  ${XDG_DOCUMENTS_DIR:-$HOME/doc} # was supposed to be do but it's a shell reserved so its *d*oc*s*
no  ${XDG_DOCUMENTS_DIR:-$HOME/doc/notes}
ed  $HOME/edu
in  ${XDG_DOWNLOAD_DIR:-$HOME/inc}
me  ${XDG_MEDIA_DIR:-$HOME/med}
pic  ${XDG_MEDIA_DIR:-$HOME/med/pics}
mov  ${XDG_MEDIA_DIR:-$HOME/med/movies}
so  ${XDG_PROJECTS_DIR:-$HOME/src} # so like source
cf  ${XDG_CONFIG_HOME:-$HOME/.config}
se  $HOME/.local/bin 
src $HOME/.local/src
pat $HOME/.local/src/patches
sa $HOME/.local/share
sc $HOME/.local/share/captures
lo $HOME/.local
cac ${XDG_CACHE_HOME:-$HOME/.cache}
mn  /mnt
et  /etc
